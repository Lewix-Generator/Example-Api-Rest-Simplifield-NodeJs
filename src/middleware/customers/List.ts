/**
  * 
  * Middleware customers List
  * Middle to List customerss
  * 
  */
import { API_RESPONSE_STATUS, patternReponse } from "../pattern/apiResponse";

export default async (req, res, next) => {
  const {  } = req.fields;


  next();
}